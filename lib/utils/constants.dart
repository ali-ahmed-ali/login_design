import 'package:flutter/material.dart';

// App Colors
final Color darkColor = Color(0xFFFFCF54);
final Color lightColor = Color(0xFFE6E6E6);

class Conststants {
  BuildContext context;
  Conststants({required this.context});

  //double _fullWidth() => MediaQuery.of(context).size.width;
  //double _fullHeight() => MediaQuery.of(context).size.height;

}